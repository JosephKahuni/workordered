import { VerifyUniqueDirective } from './verify-unique.directive';

describe('VerifyUniqueDirective', () => {
  it('should create an instance', () => {
    const directive = new VerifyUniqueDirective();
    expect(directive).toBeTruthy();
  });
});
