import { VerifyUserExistsDirective } from './verify-user-exists.directive';

describe('VerifyUserExistsDirective', () => {
  it('should create an instance', () => {
    const directive = new VerifyUserExistsDirective();
    expect(directive).toBeTruthy();
  });
});
