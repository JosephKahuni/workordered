import { CompareFieldsDirective } from './compare-fields.directive';

describe('CompareFieldsDirective', () => {
  it('should create an instance', () => {
    const directive = new CompareFieldsDirective();
    expect(directive).toBeTruthy();
  });
});
